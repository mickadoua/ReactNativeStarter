import ActionType from "./ActionType";
//import {tcp} from "../services/tcp"; //example...

export const setConfigurationServer = (server) => ({ //example...
    type: ActionType.SET_CONFIGURATION_SERVER,
    server,
});
